'use strict';
console.log( "DEBUG: starting autofiller on " + window.location.href );

/*
 * This script takes care of the page displaying functions, such as right click menus and the autofill prompt.
 * It (will) work(s) as the frontend to the autofill scripts.
 */

